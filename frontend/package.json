{
  "name": "ai-overhaul-minimal",
  "version": "0.2.0-reset",
  "private": true,
  "description": "Minimal reset of AI Overhaul plugin frontend (context-aware button only)",
  "scripts": {
    "build": "node build.js",
    "clean": "rimraf dist || rmdir /s /q dist"
  },
  "devDependencies": {
    "@types/react": "^19.1.9",
    "dotenv": "^16.4.5",
    "typescript": "^5.9.2"
  }
}
