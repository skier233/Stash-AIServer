### Configuration sample for stash-ai-server
# Copy this to `config.env` (or another filename) and fill in secrets/paths.
# Keep the real file out of source control (see .gitignore).

# Directory where writable app data lives (overrides default /app/data)
AI_SERVER_DATA_DIR=/app/data

# Optional explicit sqlite db path (overrides DATA_DIR)
# AI_SERVER_DB_PATH=/app/data/app.db

# Directory containing plugins (if not the default /app/stash_ai_server/plugins)
# AI_SERVER_PLUGINS_DIR=/app/plugins

# Stash API key (sensitive)
# AI_SERVER_STASH_API_KEY=your-stash-api-key-here

# Optional: force the reported version (otherwise derived from installed package)
# AI_SERVER_VERSION=

# Optional: additional environment hints
# AI_SERVER_CONTAINER=1
